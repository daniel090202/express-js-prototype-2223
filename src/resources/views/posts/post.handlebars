<div class="mt-4">
    <div class="row">
        <div class="col-md-4 col-lg-3">
            <button>Like post</button>
        </div>

        <div class="col-md-8 col-lg-9">
            <h2>{{post.title}}</h2>
            <img alt="Post's Image" src="{{post.image}}" />
            <p>{{post.author}}</p>
        </div>
    </div>
</div>